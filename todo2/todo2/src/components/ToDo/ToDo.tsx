import React, { Component } from 'react';

export default class ToDo extends Component {
    //  track the states of isCompleted and isEditable
    //  connected to style rendering and number of remaining tasks
    //  track the click of an edit/save button to toggle isEditable

    render() {
        return (
            <h2>ToDo</h2>

        )
    }
}